# self — ce dépôt, évalué par son propre outil

Fabriqué par `python3 scripts/self-profile.py` (API GitHub + `.claude/`). Aucune pièce
déclarative, aucune analyse Sonar : `available` le dit. Verdict attendu et lu dans
`docs/methode.md` — il change à chaque régénération, c'est le principe.
