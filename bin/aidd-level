#!/usr/bin/env php
<?php

declare(strict_types=1);

require __DIR__.'/../vendor/autoload.php';

use AiddLevel\Infrastructure\Console\ApplicationFactory;

// Wiring lives in the Infrastructure layer (docs/specs/00-vue-ensemble.md § 4.4: no DI
// container, a wiring class is enough).
$application = ApplicationFactory::createConsoleApplication();
$application->run();
